'use strict';

/**
 * institution-contact service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::institution-contact.institution-contact');
