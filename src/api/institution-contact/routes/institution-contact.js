'use strict';

/**
 * institution-contact router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::institution-contact.institution-contact');
